# fixtures

Captured output from running scripts in `../probes/`. Each file is named
`<probe-script-name>_<UTC-timestamp>.json` and contains the raw observation
that probe made — nothing curated, nothing asserted.

Empty until a probe has been run.
